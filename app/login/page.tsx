"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function Login(){const router=useRouter();const [phone,setPhone]=useState("");const [challenge,setChallenge]=useState<string|null>(null);const [otp,setOtp]=useState("");const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);
async function requestOtp(){setLoading(true);setMessage("");try{const r=await fetch("http://localhost:8000/api/v1/auth/otp/request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phone_number:phone.trim()})});const d=await r.json();if(!r.ok)throw Error(d.detail||"Unable to send OTP");setChallenge(d.challenge_id);setMessage(d.dev_otp?`Development OTP: ${d.dev_otp}`:"OTP sent to your phone.")}catch(e:any){setMessage(e.message)}finally{setLoading(false)}}
async function verify(){if(!challenge)return;setLoading(true);setMessage("");try{const r=await fetch("http://localhost:8000/api/v1/auth/otp/verify",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({challenge_id:challenge,phone_number:phone.trim(),otp:otp.trim()})});const d=await r.json();if(!r.ok)throw Error(d.detail||"Invalid OTP");localStorage.setItem("vuryfy_token",d.access_token);router.push("/")}catch(e:any){setMessage(e.message)}finally{setLoading(false)}}
return <main className="shell"><nav><div className="brand">Vuryfy</div></nav><section className="hero"><p className="eyebrow">WELCOME BACK</p><h1>{challenge?"Enter your code.":"Sign in to Vuryfy."}</h1><p className="sub">{challenge?"Use the one-time code sent to your phone.":"Use your phone number. No password required."}</p><div className="panel">{!challenge?<><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone number" inputMode="tel"/><button disabled={loading||phone.trim().length<8} onClick={requestOtp}>{loading?"Sending…":"Continue"}</button></>:<><input value={otp} onChange={e=>setOtp(e.target.value)} placeholder="6-digit OTP" inputMode="numeric" autoFocus/><button disabled={loading||otp.length<4} onClick={verify}>{loading?"Verifying…":"Verify & continue"}</button><button className="back-action" onClick={()=>{setChallenge(null);setMessage("")}}>Use another number</button></>}{message&&<p className="message">{message}</p>}</div></section></main>}
